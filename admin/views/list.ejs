<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
</head>
<body>
  <h1><%= title %></h1>
  <% if (users.length > 0) { %>
    <ul>
      <% users.forEach(user => { %>
        <li>
          <%= user %> - <a href="/user/delete/<%= user %>">Delete</a>
        </li>
      <% }) %>
    </ul>
  <% } else { %>
    <p>No users added yet.</p>
  <% } %>
  <p><a href="/user/add">Add Another User</a></p>
  <p><a href="/">Go back to Home</a></p>
</body>
</html>